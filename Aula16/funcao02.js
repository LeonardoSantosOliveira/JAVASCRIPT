function soma(n1=0, n2=0){//n1=0 ou n2=0 são chamados de parametro opcionais, caso alguma valor nao seja passado para ele
    return n1 + n2
}

console.log(soma(2,5)) // parametro opcional seria caso alguma valor nao tivesse sido passado